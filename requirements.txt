streamlit>=1.36.0
yfinance>=0.2.40
pandas>=2.0.0
plotly>=5.20.0

# --- 실시간 뉴스 수집 + AI 번역 기능(modules/news_crawler.py)에 필요한 패키지 ---
feedparser>=6.0.10           # 구글 뉴스 RSS 피드를 읽어오는 라이브러리
requests>=2.31.0             # RSS 피드를 안전하게(timeout 포함) 내려받기 위한 라이브러리
google-generativeai>=0.8.0   # Google Gemini API로 뉴스를 한국어로 번역/요약 (gemini-1.5-flash)
python-dotenv>=1.0.0         # 로컬 개발 시 .env 파일에서 API 키를 불러오기 위한 라이브러리
