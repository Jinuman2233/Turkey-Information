streamlit>=1.36.0
yfinance>=0.2.40
pandas>=2.0.0
plotly>=5.20.0

# --- 실시간 뉴스 수집 + AI 번역 기능(modules/news_crawler.py)에 필요한 패키지 ---
feedparser>=6.0.10       # 구글 뉴스 RSS 피드를 읽어오는 라이브러리
requests>=2.31.0         # RSS 피드를 안전하게(timeout 포함) 내려받기 위한 라이브러리
openai>=1.40.0           # OpenAI API로 뉴스를 한국어로 번역/요약 (기본 AI 제공자)
python-dotenv>=1.0.0     # 로컬 개발 시 .env 파일에서 API 키를 불러오기 위한 라이브러리

# --- (선택) Gemini API를 사용하고 싶을 때만 아래 주석을 해제하고 설치하세요 ---
# AI_PROVIDER=gemini 로 설정했을 때 필요합니다. (기본값인 OpenAI만 쓴다면 설치하지 않아도 됩니다)
# google-genai>=0.3.0
